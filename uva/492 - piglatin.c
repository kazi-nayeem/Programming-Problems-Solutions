#include<stdio.h>
#include<string.h>

#define MAX 105

int main()
{
    int len, i, j, k;
    int flag;
    char string[MAX];
    char piglatin[MAX];
    while(gets(string))
    {
        len = strlen(string);
        if(len == 0) break;

    }
}
