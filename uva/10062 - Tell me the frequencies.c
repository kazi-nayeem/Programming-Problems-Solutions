#include<stdio.h>
#include<string.h>
#include<stdlib.h>

int comp (const void *a, const void *b)
{
    return (*(int*) a - *(int*) b);
}
int main()
{
    int len, count;
    char sentence[1005];
    while(gets(sentence))
    {
        len = strlen(sentence);
    }
}
